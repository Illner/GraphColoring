using System;
using System.Text;

namespace GraphColoring.Tests
{
    interface ITestInterface
    {
        // Method
        StringBuilder Test();
        // StringBuilder Test(Enum @enum);
        string GetPath();
    }
}
